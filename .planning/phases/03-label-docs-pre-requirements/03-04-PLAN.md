---
phase: 03-label-docs-pre-requirements
plan: "04"
type: tdd
wave: 3
depends_on:
  - 03-01
  - 03-02
  - 03-03
files_modified:
  - pipeline/smart_ac_verifier.py
  - tests/test_label_flows.py
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-04
  - DOC-05

must_haves:
  truths:
    - "_MCSL_WORKFLOW_GUIDE contains all five document verification strategies as distinct named sections"
    - "DOC-01: 'LABEL CREATED' badge check steps documented with exact status locator"
    - "DOC-02: Download Documents ZIP strategy documented with download_zip action reference"
    - "DOC-03: Label request XML/JSON strategy documented with Label Summary 3-dots → View Log → dialogHalfDivParent"
    - "DOC-04: Print Documents new-tab strategy explicitly says switch_tab + screenshot + close_tab (NOT download_zip)"
    - "DOC-05: Rate log strategy documented with ViewallRateSummary expand step before 3-dots"
    - "All five doc strategy tests (test_doc01 through test_doc05) pass"
  artifacts:
    - path: "pipeline/smart_ac_verifier.py"
      provides: "_MCSL_WORKFLOW_GUIDE with Document Verification Strategies section (all 5 strategies)"
    - path: "tests/test_label_flows.py"
      provides: "test_doc01_badge_check through test_doc05_rate_log active and passing"
  key_links:
    - from: "_MCSL_WORKFLOW_GUIDE DOC-04 section"
      to: "_do_action() switch_tab handler"
      via: "guide instructs: Print Documents → switch_tab (NOT download_zip)"
      pattern: "switch_tab"
    - from: "_MCSL_WORKFLOW_GUIDE DOC-05 section"
      to: "rate log locators"
      via: "ViewallRateSummary expand → 3-dots → dialogHalfDivParent"
      pattern: "ViewallRateSummary"
---

<objective>
Add all five document verification strategies to _MCSL_WORKFLOW_GUIDE and activate their tests.

Purpose: The document verification strategies (DOC-01 through DOC-05) are the core evidence-gathering mechanisms the agent uses after label generation. Without explicit guide entries, the agent will attempt incorrect paths (e.g., download_zip for Print Documents, or missing the ViewallRateSummary expand step for rate logs).

Output: _MCSL_WORKFLOW_GUIDE extended with a "Document Verification Strategies" section covering all five approaches; all five doc tests active and passing.
</objective>

<execution_context>
@/Users/madan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/madan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-label-docs-pre-requirements/03-RESEARCH.md
@.planning/phases/03-label-docs-pre-requirements/03-01-SUMMARY.md
</context>

<interfaces>
<!-- Exact locators from orderSummaryPage.ts (Research Patterns 8 + Code Examples) -->

DOC-01 — Badge check (after Generate Label):
```
Status span: appFrame.locator('div[class="order-summary-greyBlock"] > div:nth-child(1) > div:nth-child(1) > div > span')
Status button (for polling): appFrame.getByRole('button').nth(2)  → text = "LABEL CREATED"
Label Summary visible: appFrame.getByText('Label Summary')
SUCCESS cell: appFrame.getByRole('cell', name='SUCCESS').first()
```

DOC-02 — Download Documents ZIP:
```
action: download_zip, target: "Download Documents"
Result: action["_zip_content"] dict with file names and content
NOTE: Exact button text in MCSL is uncertain — may differ from FedEx. Agent should probe AX tree
      on Order Summary (after LABEL CREATED) for the exact "Download Documents" text.
```

DOC-03 — Label Request XML/JSON (3-dots on Label Summary row):
```
After LABEL CREATED, in the Label Summary table:
1. Click 3-dots (⋯) on the label row:
   appFrame.locator('div[class="order-summary-root"]>div>div:nth-child(2)>div>div>div:nth-child(3)>div>div>div:nth-child(2)>div>table>tbody>tr>td:nth-child(8)')
   → .nth(rowIndex) for specific row
2. Click "View Log" menuitem:
   appFrame.locator('div[role="presentation"]>div:nth-child(2)>ul>li:nth-child(1)').first()
3. Wait for dialog: appFrame.locator('.dialogHalfDivParent').nth(labelreq) → state: visible
4. Read text: labelRequestSummary.textContent() → strip whitespace → verify XML/JSON field names present
5. Close: appFrame.locator('body > div:nth-child(15) > ... > button > div') (closeLabelRequestSummary)
NOTE: This is the MCSL flow. The FedEx "How To → Click Here → ZIP" flow does NOT exist in MCSL.
```

DOC-04 — Print Documents new-tab screenshot:
```
1. Click "Print Documents" button on Order Summary (standalone, NOT in More Actions menu)
2. New tab opens at *.pluginhive.io or *.pluginhive.com domain
3. action: switch_tab → active_page switches to new tab
4. Take screenshot → Claude reads label codes (ICE, ALCOHOL, ELB, ASR, DSR)
5. action: close_tab → returns to Order Summary
CRITICAL: Do NOT use download_zip. Print Documents is a NEW TAB, not a file download.
```

DOC-05 — Rate log screenshot (BEFORE label generation):
```
On Order Summary after rates load, BEFORE clicking Generate Label:
1. Click ViewallRateSummary: appFrame.getByTitle('View all Rate Summary')
   (MUST expand first — table is collapsed by default)
2. Wait for rate summary table container: appFrame.locator('.rate-summary-table-container')
3. Click 3-dots on rate summary row:
   appFrame.locator('.rate-summary-table tbody tr td:last-child button[aria-haspopup="true"]')
4. Click View Log menuitem:
   appFrame.locator('div[role="presentation"]>div:nth-child(2)>ul>li:nth-child(1)').first()
5. Log dialog visible: appFrame.locator('.dialogHalfDivParent')
6. Screenshot the dialog → Claude verifies JSON/XML fields
7. Close: appFrame.getByRole('button', name='Close')
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DOC-01 and DOC-04 guide sections + tests</name>
  <files>pipeline/smart_ac_verifier.py, tests/test_label_flows.py</files>
  <behavior>
    - test_doc01_badge_check (unskip): assert _MCSL_WORKFLOW_GUIDE contains "LABEL CREATED", "order-summary-greyBlock" locator (or "status" reference), and "Label Summary" check
    - test_doc04_print_documents (unskip): assert _MCSL_WORKFLOW_GUIDE contains "Print Documents", "switch_tab", and explicitly states NOT to use download_zip for Print Documents
    - Both are content assertions on the guide string
  </behavior>
  <action>
    RED: Unskip test_doc01_badge_check and test_doc04_print_documents. Run — both must fail.

    GREEN: Add a "Document Verification Strategies" section to _MCSL_WORKFLOW_GUIDE after the label flow sections. Add subsections:

    DOC-01 subsection:
    ```
    ### DOC-01: Label Existence Badge Check
    After "Generate Label" click, verify status reaches LABEL CREATED:
    - Status span: div[class="order-summary-greyBlock"] > div:nth-child(1) > div:nth-child(1) > div > span
    - Status button (for polling): getByRole('button').nth(2) shows "LABEL CREATED"
    - Label Summary table visible: getByText('Label Summary')
    - SUCCESS cell visible: getByRole('cell', name='SUCCESS').first()
    ```

    DOC-04 subsection:
    ```
    ### DOC-04: Print Documents (New Tab Screenshot)
    ⚠️ IMPORTANT: Print Documents opens a NEW TAB — do NOT use download_zip here.
    Steps:
    1. Click "Print Documents" button on Order Summary (standalone button)
    2. New tab opens at *.pluginhive.io domain
    3. action: switch_tab → active_page = new tab
    4. Take screenshot → Claude reads label service codes visible on label (ICE, ALCOHOL, ELB, ASR, DSR)
    5. action: close_tab → returns to Order Summary
    ```

    Commit sequence:
    1. test(03-04): unskip test_doc01_badge_check and test_doc04_print_documents
    2. feat(03-04): add DOC-01 and DOC-04 sections to _MCSL_WORKFLOW_GUIDE
  </action>
  <verify>
    <automated>cd /Users/madan/Documents/MCSLDomainExpert && PYTHONPATH=. .venv/bin/pytest tests/test_label_flows.py::test_doc01_badge_check tests/test_label_flows.py::test_doc04_print_documents -x -q</automated>
  </verify>
  <done>test_doc01_badge_check and test_doc04_print_documents pass; "switch_tab" and "NOT" download_zip warning appear in DOC-04 section</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DOC-02, DOC-03, DOC-05 guide sections + tests</name>
  <files>pipeline/smart_ac_verifier.py, tests/test_label_flows.py</files>
  <behavior>
    - test_doc02_download_zip (unskip): assert _MCSL_WORKFLOW_GUIDE contains "Download Documents", "download_zip", and "_zip_content"
    - test_doc03_label_request_xml (unskip): assert _MCSL_WORKFLOW_GUIDE contains "Label Summary", "click3DotsOnLabelSummary" (or "td:nth-child(8)"), "View Log", and "dialogHalfDivParent"
    - test_doc05_rate_log (unskip): assert guide contains "ViewallRateSummary" (or "View all Rate Summary"), "dialogHalfDivParent", and the note that table must be expanded BEFORE clicking 3-dots
  </behavior>
  <action>
    RED: Unskip test_doc02_download_zip, test_doc03_label_request_xml, test_doc05_rate_log. Run — all three must fail.

    GREEN: Add three more subsections to the Document Verification Strategies section in _MCSL_WORKFLOW_GUIDE:

    DOC-02 subsection:
    ```
    ### DOC-02: Download Documents ZIP
    On Order Summary after LABEL CREATED:
    1. Find "Download Documents" button (text may vary — probe AX tree if not found)
    2. action: download_zip, target: "Download Documents"
    3. Result: action["_zip_content"] populated with file names and content snippets
    4. Claude verifies expected document files are present (PDF size notes, CSV content, etc.)
    ```

    DOC-03 subsection:
    ```
    ### DOC-03: Label Request XML/JSON (3-dots on Label Summary row)
    After LABEL CREATED, in the Label Summary table:
    1. Click 3-dots (⋯) on the label row:
       appFrame TD selector: div[class="order-summary-root"]>...>tbody>tr>td:nth-child(8)
       Use .nth(rowIndex) for a specific row
    2. Click "View Log" menuitem:
       appFrame.locator('div[role="presentation"]>div:nth-child(2)>ul>li:nth-child(1)').first()
    3. Wait for .dialogHalfDivParent to be visible
    4. Read textContent() → strip whitespace → verify expected XML/JSON field names are present
    5. Close dialog via closeLabelRequestSummary button
    NOTE: Do NOT use download_zip here — this is a dialog, not a file download. FedEx "How To" flow does NOT apply to MCSL.
    ```

    DOC-05 subsection:
    ```
    ### DOC-05: Rate Log Screenshot (BEFORE label generation)
    On Order Summary, after rates load, BEFORE clicking Generate Label:
    1. FIRST: Click ViewallRateSummary (getByTitle 'View all Rate Summary') — table is COLLAPSED by default
    2. Wait for .rate-summary-table-container to be visible
    3. Click 3-dots button on rate summary row:
       .rate-summary-table tbody tr td:last-child button[aria-haspopup="true"]
    4. Click View Log menuitem:
       div[role="presentation"]>div:nth-child(2)>ul>li:nth-child(1) (first)
    5. Log dialog: .dialogHalfDivParent — contains JSON/XML request text
    6. Screenshot dialog → Claude verifies required fields
    7. Close: getByRole('button', name='Close')
    WARNING: Must expand ViewallRateSummary FIRST — 3-dots button is not visible on collapsed table.
    ```

    Commit sequence:
    1. test(03-04): unskip test_doc02_download_zip, test_doc03_label_request_xml, test_doc05_rate_log
    2. feat(03-04): add DOC-02, DOC-03, DOC-05 sections to _MCSL_WORKFLOW_GUIDE
  </action>
  <verify>
    <automated>cd /Users/madan/Documents/MCSLDomainExpert && PYTHONPATH=. .venv/bin/pytest tests/test_label_flows.py::test_doc02_download_zip tests/test_label_flows.py::test_doc03_label_request_xml tests/test_label_flows.py::test_doc05_rate_log -x -q</automated>
  </verify>
  <done>All three tests pass; DOC-02/03/05 sections in guide; ViewallRateSummary expand warning present; full suite green</done>
</task>

</tasks>

<verification>
PYTHONPATH=. .venv/bin/pytest tests/ -q — all active tests pass
grep -n "DOC-01\|DOC-02\|DOC-03\|DOC-04\|DOC-05" pipeline/smart_ac_verifier.py — confirms 5 strategy sections
grep -n "ViewallRateSummary\|View all Rate Summary" pipeline/smart_ac_verifier.py — DOC-05 expand step present
grep -n "switch_tab" pipeline/smart_ac_verifier.py — DOC-04 tab strategy present
</verification>

<success_criteria>
- _MCSL_WORKFLOW_GUIDE has a "Document Verification Strategies" section with all 5 named subsections
- DOC-04 section explicitly warns against using download_zip (uses switch_tab instead)
- DOC-05 section requires ViewallRateSummary expand BEFORE 3-dots click
- All 5 doc tests (test_doc01 through test_doc05) active and passing
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/03-label-docs-pre-requirements/03-04-SUMMARY.md`
</output>
