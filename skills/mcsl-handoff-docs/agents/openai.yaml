display_name: MCSL Handoff Docs
short_description: Generate professional Support Guide and Business Brief PDFs for approved MCSL release cards.
default_prompt: Generate the requested MCSL handoff document from the approved card context. If I ask for one doc, create only that one; if I ask for PDF, render it using the dashboard handoff PDF style.
